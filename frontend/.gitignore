# Environment variables and secrets (CRITICAL - never commit these)
.env*
!.env.example

# Dependencies (keep these local)
node_modules/

# Next.js build artifacts (keep these local)
.next/
out/
build/

# Debug logs (keep these local)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Vercel deployment
.vercel

# TypeScript build info (keep these local)
*.tsbuildinfo
next-env.d.ts

# IDE and editor files (keep these local)
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
tmp/
temp/
*.tmp
*.temp

# Cache directories
.cache
.parcel-cache
.eslintcache
.npm

# Test coverage (keep these local)
coverage/
.nyc_output/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity