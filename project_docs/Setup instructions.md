# Setup instructions

```markdown
go to terminal

first time instructions

cd backend;
python3.11 -m venv venv    //remove if there is already virtual environment
python.exe -m pip install --upgrade pip
pip install -r requirements.txt

cd backend;

venv/Scripts/activate
python main.py

cd frontend;
npm run dev
```